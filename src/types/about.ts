export type AboutHero = {
  eyebrow: string;
  title: string;
  description: string;
  image: string;
  imageAlt: string;
};

export type StoryBlock = {
  title: string;
  description: string;
};

export type ValueItem = {
  title: string;
  description: string;
};

export type TrustIndicator = {
  title: string;
  description: string;
};

export type FamilyPillar = {
  title: string;
  description: string;
};

export type StatItem = {
  label: string;
  value: string;
  note: string;
};
